apiVersion: v1
kind: Service
metadata:
  name: hello
  labels:
    app: hello
spec:
  replicas: 1
  selector:
    app: hello
  template:
    metadata:
      labels:
        app: hello
    spec:
      containers:
      - name: hello
        image: hello:latest
        ports:
        - containerPort: 5000
